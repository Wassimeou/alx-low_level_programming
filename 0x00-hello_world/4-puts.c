#include <studio.h>
/**
 *main - main entry point
 *return : always 0 (success)
 */
int main(void)
{             
        puts("\"Programming is like building a multilingal puzzle");
        return (0);
}
